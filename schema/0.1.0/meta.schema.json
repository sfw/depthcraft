{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://depthcraft.dev/schema/0.1.0/meta.schema.json",
  "title": "DepthcraftLessonMeta",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "lessonId", "anchors"],
  "properties": {
    "schemaVersion": { "const": "0.1.0" },
    "lessonId": { "type": "string", "minLength": 1 },
    "anchors": {
      "type": "array",
      "description": "Heading-derived stubs in v0.1 so v0.2 primers attach without rewriting lessons.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "heading", "kind"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "heading": { "type": "string", "minLength": 1 },
          "kind": { "type": "string", "enum": ["section", "concept", "warning"] }
        }
      }
    }
  }
}
